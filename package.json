{
  "name": "typing_speed_website",
  "version": "1.1.0",
  "description": "A website that you can test your typing speed.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd page && npm install --only=dev && npm install && npm run build"
  },
  "author": "Brian MacMonigle",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "fetch-cookie": "^0.7.0",
    "node-fetch": "^1.7.3",
    "path": "^0.12.7"
  }
}
